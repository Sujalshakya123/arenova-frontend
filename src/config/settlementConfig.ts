/**
 * Tournament settlement (ledger after completion).
 *
 * UNDO: set SETTLEMENT_ENABLED to false and restart the frontend dev server.
 * Also set arenova.settlement.enabled=false in application.properties and restart backend.
 */
export const SETTLEMENT_ENABLED = true;
